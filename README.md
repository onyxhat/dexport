# dexport
Export running Docker containers as a docker-compose.yml file.  docker-export inspects your running containers and generates a ready-to-use Compose file from their actual configuration — ports, volumes, environment variables, networks, restart policies, resource limits, and more.
